/**
 * AUTO-STUB — Memory routes (snapshot/outcome/attribution audit).
 * Preserved only as no-op so the engine boots; routes are not exposed.
 */
import type { FastifyInstance } from 'fastify';
export const memoryRoutes = async (_app: FastifyInstance): Promise<void> => { /* noop */ };
export default memoryRoutes;
