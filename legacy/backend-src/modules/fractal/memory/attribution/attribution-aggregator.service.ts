/**
 * AUTO-STUB — Attribution aggregator (post-hoc reasons stats).
 */
export const attributionAggregatorService = {
  aggregate:    async (..._args: any[]) => ({ ok: true, aggregated: 0, stubbed: true }),
  start:        () => { /* noop */ },
  stop:         () => { /* noop */ },
};
export default attributionAggregatorService;
