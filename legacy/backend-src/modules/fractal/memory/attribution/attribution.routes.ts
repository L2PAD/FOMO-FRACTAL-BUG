/**
 * AUTO-STUB — Attribution audit routes.
 */
import type { FastifyInstance } from 'fastify';
export const attributionRoutes = async (_app: FastifyInstance): Promise<void> => { /* noop */ };
export default attributionRoutes;
